import { DialogConfig } from '../../providers/DialogProvider';
import RequestDemoDialog from './RequestDemoDialog';

export const dialogsConfig: DialogConfig[] = [
  {
    id: 'request-demo',
    component: RequestDemoDialog,
  },
];
